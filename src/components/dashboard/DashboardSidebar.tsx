// DashboardSidebar — Legacy component no longer used.
// Role-specific sidebars are now embedded in:
// - dashboard.admin.tsx
// - dashboard.owner.tsx
// - dashboard.labour.tsx (no sidebar, mobile-first)
export function DashboardSidebar() {
  return null;
}
